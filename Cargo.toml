[workspace]
# members = ["crates/*"]

[workspace.dependencies]
bevy = { version = "0.19.0", default-features = false, features = ["3d", "audio", "ui"] }

[package]
name = "prop-hunt"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy.workspace = true
